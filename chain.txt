(LOCALE)

set AWS_REGION=us-east-2
set AWS_SDK_LOAD_CONFIG=1
set AWS_ACCESS_KEY_ID=
set AWS_SECRET_ACCESS_KEY=

go run http-api.go pubsub-system.go dynamo-db-repository.go      (senza contesto)
go run http-api.go pubsub-system.go dynamo-db-repository.go ctx  (con contesto)


(Per mettere EB)

https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/eb-cli3-install-windows.html



(AWS EB)

eb init
eb create
eb terminate


(DOCKER)

docker build -t wbmqsystem .
docker run -d -p 5000:5000 -e AWS_REGION=us-east-2 -e AWS_SDK_LOAD_CONFIG=1 -e AWS_ACCESS_KEY_ID="id" -e AWS_SECRET_ACCESS_KEY="psw" wbmqsystem
